# Client Role Configuration
# Format: common_name=role
# Roles: ADMIN, OPERATOR, VIEWER, MAINTENANCE

# Administrators - full system access
admin_client=ADMIN
system_admin=ADMIN

# Operators - monitor and acknowledge alerts
operator_client=OPERATOR
shift_supervisor=OPERATOR
control_room=OPERATOR

# Viewers - read-only access
viewer_client=VIEWER
dashboard_display=VIEWER
report_generator=VIEWER

# Maintenance personnel
maintenance_client=MAINTENANCE
technician=MAINTENANCE

# Example for multiple clients
# plant_manager=ADMIN
# line_operator_1=OPERATOR
# line_operator_2=OPERATOR
# quality_inspector=VIEWER
# service_tech=MAINTENANCE
